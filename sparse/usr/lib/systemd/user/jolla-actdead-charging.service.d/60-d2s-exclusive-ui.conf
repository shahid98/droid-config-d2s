[Unit]
# Both UI services have Restart=always.  Make the charging-only compositor and
# normal Lipstick explicitly exclusive so a low-battery runlevel transition
# cannot alternate them on the display.
Conflicts=lipstick.service
